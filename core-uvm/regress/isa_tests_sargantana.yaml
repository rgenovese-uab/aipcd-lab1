name: sargantana_isa_check
description: SARGANTANA core RISC-V isa tests
type: test

tests:
    build: compile-env
    rtype: set
    test: tests/build/isa_tests/
    cmd: make run
    args:
        - CORE_TYPE=sargantana
