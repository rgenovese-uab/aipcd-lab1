name: ka_random_check
description: Ka core Riscv-dv random binaries regression
type: test

tests:
    rtype: set
    test: tests/build/generated_tests/
    cmd: make run
    args:
        - SEED=random
